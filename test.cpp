#include <iostream>
#include <vector>
int main(){
  unsigned long long = -1;
  std::vector<int> myVector(x);

    for (int i = 0; i < x; i++) {
        myVector[i] = i * 2; 
    }
    for (int i = 0; i < x; i++) {
        std::cout << myVector[i] << " ";
 
    }
  return 0;
}
